# Weather Dashboard API

A Python script that fetches real-time weather data for any city using the Open-Meteo API (no API key required).

## Usage
```bash
pip install -r requirements.txt
python weather.py
```
